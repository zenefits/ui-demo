import React from 'react';

import { Box, TextBlock } from '../index';

export default () => (
  <Box w={400}>
    <TextBlock tag="p">
      Richard Branson, founder of the Virgin Group, implemented an unlimited “policy-that-isn’t” where employees can
      take as much time off as they want – without prior approval. Also, the amount of vacation time a person takes
      isn’t tracked.
    </TextBlock>
  </Box>
);
