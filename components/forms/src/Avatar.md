Using only `firstName` and `lastName` props:

```jsx
// loadExample('./Avatar/exampleDefault')
```

Note that `photoUrl` takes precedence, and that common utility props like `s` are also available:

```jsx
// loadExample('./Avatar/examplePhoto')
```
