# tslint-config-z-frontend

Provides an eslint configuration to be used by packages in this repo.

## Installation

Each package that uses typescript needs `tslint` and `typescript` packages.
By default, packages will use configuration in `tslint.json` at the root.
