import { Form } from 'z-frontend-forms';
import { My } from '../Form';

cosnt d1 = <Form<string> onSubmit={2} />;

cosnt d2 = <Form onSubmit={1} />;
           ~~~~~~~~~~~~~~~~~~~~~            ["Form" component must have type argument (e.g. <Form<MyType> ... >)]


cosnt d3 = <My.Select<string> onSubmit={2} />;

cosnt d4 = <My.Select onSubmit={1} />;
           ~~~~~~~~~~~~~~~~~~~~~~~~~~        ["My.Select" component must have type argument (e.g. <My.Select<MyType> ... >)]


