import { mockGraphQL } from 'z-frontend-cypress/utils';

describe('Overview Page', () => {
  it.skip('Example test, please rename this file and add a meaningful test', () => {
    mockGraphQL({});
    cy.visit('/');
  });
});
